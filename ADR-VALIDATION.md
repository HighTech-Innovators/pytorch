# ADR Validation Report

Run: 3
Date: 2026-07-08

## Results

| Check | Status | Notes |
|---|---|---|
| 1. Scope map current | PASS | all present |
| 2. Files match COVERED | PASS | 43 ADR.md files found under ./src; none under ./src/src; count matches 43 COVERED entries |
| 3. Exclusion justifications | FAIL | 49 invalid EXCLUDED rows in src/adr-scope.md (named-in-book and/or line-count violations); see Required Actions |
| 4. ADR content non-stub | PASS | 43 ADRs validated; grep -rn '\.\.' found 6 non-link matches and 0 '../' dependency links; all ADR link targets exist |
| 5. Book cross-reference | FAIL | aten/src/ATen/core=COVERED via aten/src/ATen/core; aten/src/ATen/core/dispatch=COVERED via aten/src/ATen/core; aten/src/ATen/cpu/vec=NOT COVERED; aten/src/ATen/native=COVERED via aten/src/ATen/native; aten/src/ATen/native/cpu=COVERED via aten/src/ATen/native/cpu; aten/src/ATen/native/cuda=COVERED via aten/src/ATen/native/cuda; aten/src/ATen/native/quantized=COVERED via aten/src/ATen/native/quantized; aten/src/ATen/native/sparse=COVERED via aten/src/ATen/native/sparse; c10/core=COVERED via c10/core; c10/cuda=COVERED via c10/cuda; c10/mobile=COVERED via c10/mobile; c10/util=COVERED via c10/util; caffe2/core=COVERED via caffe2/core; functorch=EXCLUDED; tools/autograd=COVERED via tools/autograd; torch/_decomp=COVERED via torch/_decomp; torch/_dynamo=COVERED via torch/_dynamo; torch/_dynamo/variables=COVERED via torch/_dynamo; torch/_export=COVERED via torch/_export; torch/_functorch=COVERED via torch/_functorch; torch/_functorch/_aot_autograd=COVERED via torch/_functorch; torch/_inductor=COVERED via torch/_inductor; torch/_inductor/codegen=COVERED via torch/_inductor/codegen; torch/_inductor/fx_passes=COVERED via torch/_inductor; torch/amp=COVERED via torch/amp; torch/autograd=COVERED via torch/autograd; torch/csrc=NOT COVERED; torch/csrc/api=COVERED via torch/csrc/api; torch/csrc/autograd=COVERED via torch/csrc/autograd; torch/csrc/distributed=COVERED via torch/csrc/distributed; torch/csrc/dynamo=COVERED via torch/csrc/dynamo; torch/csrc/inductor=COVERED via torch/csrc/inductor; torch/csrc/jit=COVERED via torch/csrc/jit; torch/csrc/profiler=COVERED via torch/csrc/profiler; torch/cuda=COVERED via torch/cuda; torch/distributed=COVERED via torch/distributed; torch/fx=COVERED via torch/fx; torch/fx/passes=COVERED via torch/fx; torch/nn=COVERED via torch/nn; torch/nn/modules=COVERED via torch/nn/modules; torch/onnx=COVERED via torch/onnx; torch/optim=COVERED via torch/optim; torch/profiler=COVERED via torch/profiler; torch/utils=COVERED via torch/utils; torch/utils/benchmark=COVERED via torch/utils; torchgen=COVERED via torchgen |

## Overall: FAIL

## Required Actions

- In `src/adr-scope.md:66`, `benchmarks` is EXCLUDED as `Test suite`; but it is named in book/01-repository-orientation.md:51. Do not leave `benchmarks` EXCLUDED: mark it COVERED and add `./src/benchmarks/ADR.md`.
- In `src/adr-scope.md:72`, `c10/metal` is EXCLUDED as `Build/config only`; and its immediate source-file line count violates the exclusion cap (7135 exceeds limit 2000). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `c10/metal` COVERED and add `./src/c10/metal/ADR.md`.
- In `src/adr-scope.md:74`, `c10/xpu` is EXCLUDED as `Build/config only`; and its immediate source-file line count violates the exclusion cap (3886 exceeds limit 2000). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `c10/xpu` COVERED and add `./src/c10/xpu/ADR.md`.
- In `src/adr-scope.md:75`, `caffe2` is EXCLUDED as `Leaf with no architectural boundary`; but it is named in book/01-repository-orientation.md:48. Do not leave `caffe2` EXCLUDED: mark it COVERED and add `./src/caffe2/ADR.md`.
- In `src/adr-scope.md:76`, `caffe2/perfkernels` is EXCLUDED as `Leaf with no architectural boundary`; and its immediate source-file line count violates the exclusion cap (10396 exceeds limit 200). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `caffe2/perfkernels` COVERED and add `./src/caffe2/perfkernels/ADR.md`.
- In `src/adr-scope.md:77`, `caffe2/serialize` is EXCLUDED as `Leaf with no architectural boundary`; and its immediate source-file line count violates the exclusion cap (3714 exceeds limit 200). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `caffe2/serialize` COVERED and add `./src/caffe2/serialize/ADR.md`.
- In `src/adr-scope.md:78`, `caffe2/utils` is EXCLUDED as `Leaf with no architectural boundary`; and its immediate source-file line count violates the exclusion cap (395 exceeds limit 200). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `caffe2/utils` COVERED and add `./src/caffe2/utils/ADR.md`.
- In `src/adr-scope.md:79`, `cmake` is EXCLUDED as `Build/config only`; but it is named in book/01-repository-orientation.md:50. Do not leave `cmake` EXCLUDED: mark it COVERED and add `./src/cmake/ADR.md`.
- In `src/adr-scope.md:80`, `docs` is EXCLUDED as `Build/config only`; but it is named in book/01-repository-orientation.md:52. Do not leave `docs` EXCLUDED: mark it COVERED and add `./src/docs/ADR.md`.
- In `src/adr-scope.md:83`, `functorch/dim` is EXCLUDED as `Leaf with no architectural boundary`; and its immediate source-file line count violates the exclusion cap (3487 exceeds limit 200). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `functorch/dim` COVERED and add `./src/functorch/dim/ADR.md`.
- In `src/adr-scope.md:85`, `functorch/einops` is EXCLUDED as `Leaf with no architectural boundary`; and its immediate source-file line count violates the exclusion cap (522 exceeds limit 200). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `functorch/einops` COVERED and add `./src/functorch/einops/ADR.md`.
- In `src/adr-scope.md:91`, `test` is EXCLUDED as `Test suite`; but it is named in book/01-repository-orientation.md:47, book/01-repository-orientation.md:161. Do not leave `test` EXCLUDED: mark it COVERED and add `./src/test/ADR.md`.
- In `src/adr-scope.md:92`, `third_party` is EXCLUDED as `Vendored/third-party`; but it is named in book/01-repository-orientation.md:49. Do not leave `third_party` EXCLUDED: mark it COVERED and add `./src/third_party/ADR.md`.
- In `src/adr-scope.md:93`, `tools` is EXCLUDED as `Leaf with no architectural boundary`; but it is named in book/01-repository-orientation.md:46; and its immediate source-file line count violates the exclusion cap (4282 exceeds limit 200). Do not leave `tools` EXCLUDED: mark it COVERED and add `./src/tools/ADR.md`.
- In `src/adr-scope.md:111`, `tools/stats` is EXCLUDED as `Build/config only`; and its immediate source-file line count violates the exclusion cap (2767 exceeds limit 2000). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `tools/stats` COVERED and add `./src/tools/stats/ADR.md`.
- In `src/adr-scope.md:115`, `torch` is EXCLUDED as `Leaf with no architectural boundary`; but it is named in book/06-python-bindings.md:5; and its immediate source-file line count violates the exclusion cap (61203 exceeds limit 200). Do not leave `torch` EXCLUDED: mark it COVERED and add `./src/torch/ADR.md`.
- In `src/adr-scope.md:119`, `torch/_custom_op` is EXCLUDED as `Leaf with no architectural boundary`; and its immediate source-file line count violates the exclusion cap (1032 exceeds limit 200). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `torch/_custom_op` COVERED and add `./src/torch/_custom_op/ADR.md`.
- In `src/adr-scope.md:120`, `torch/_dispatch` is EXCLUDED as `Leaf with no architectural boundary`; and its immediate source-file line count violates the exclusion cap (204 exceeds limit 200). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `torch/_dispatch` COVERED and add `./src/torch/_dispatch/ADR.md`.
- In `src/adr-scope.md:121`, `torch/_higher_order_ops` is EXCLUDED as `Leaf with no architectural boundary`; and its immediate source-file line count violates the exclusion cap (18517 exceeds limit 200). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `torch/_higher_order_ops` COVERED and add `./src/torch/_higher_order_ops/ADR.md`.
- In `src/adr-scope.md:122`, `torch/_lazy` is EXCLUDED as `Leaf with no architectural boundary`; and its immediate source-file line count violates the exclusion cap (611 exceeds limit 200). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `torch/_lazy` COVERED and add `./src/torch/_lazy/ADR.md`.
- In `src/adr-scope.md:123`, `torch/_library` is EXCLUDED as `Leaf with no architectural boundary`; and its immediate source-file line count violates the exclusion cap (5172 exceeds limit 200). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `torch/_library` COVERED and add `./src/torch/_library/ADR.md`.
- In `src/adr-scope.md:124`, `torch/_logging` is EXCLUDED as `Leaf with no architectural boundary`; and its immediate source-file line count violates the exclusion cap (2084 exceeds limit 200). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `torch/_logging` COVERED and add `./src/torch/_logging/ADR.md`.
- In `src/adr-scope.md:125`, `torch/_native` is EXCLUDED as `Leaf with no architectural boundary`; and its immediate source-file line count violates the exclusion cap (2195 exceeds limit 200). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `torch/_native` COVERED and add `./src/torch/_native/ADR.md`.
- In `src/adr-scope.md:126`, `torch/_numpy` is EXCLUDED as `Leaf with no architectural boundary`; and its immediate source-file line count violates the exclusion cap (7254 exceeds limit 200). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `torch/_numpy` COVERED and add `./src/torch/_numpy/ADR.md`.
- In `src/adr-scope.md:127`, `torch/_prims` is EXCLUDED as `Leaf with no architectural boundary`; and its immediate source-file line count violates the exclusion cap (3897 exceeds limit 200). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `torch/_prims` COVERED and add `./src/torch/_prims/ADR.md`.
- In `src/adr-scope.md:128`, `torch/_prims_common` is EXCLUDED as `Leaf with no architectural boundary`; and its immediate source-file line count violates the exclusion cap (2757 exceeds limit 200). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `torch/_prims_common` COVERED and add `./src/torch/_prims_common/ADR.md`.
- In `src/adr-scope.md:129`, `torch/_refs` is EXCLUDED as `Leaf with no architectural boundary`; and its immediate source-file line count violates the exclusion cap (7858 exceeds limit 200). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `torch/_refs` COVERED and add `./src/torch/_refs/ADR.md`.
- In `src/adr-scope.md:130`, `torch/_strobelight` is EXCLUDED as `Leaf with no architectural boundary`; and its immediate source-file line count violates the exclusion cap (546 exceeds limit 200). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `torch/_strobelight` COVERED and add `./src/torch/_strobelight/ADR.md`.
- In `src/adr-scope.md:131`, `torch/_subclasses` is EXCLUDED as `Leaf with no architectural boundary`; and its immediate source-file line count violates the exclusion cap (10599 exceeds limit 200). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `torch/_subclasses` COVERED and add `./src/torch/_subclasses/ADR.md`.
- In `src/adr-scope.md:133`, `torch/accelerator` is EXCLUDED as `Leaf with no architectural boundary`; and its immediate source-file line count violates the exclusion cap (870 exceeds limit 200). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `torch/accelerator` COVERED and add `./src/torch/accelerator/ADR.md`.
- In `src/adr-scope.md:136`, `torch/compiler` is EXCLUDED as `Leaf with no architectural boundary`; and its immediate source-file line count violates the exclusion cap (1670 exceeds limit 200). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `torch/compiler` COVERED and add `./src/torch/compiler/ADR.md`.
- In `src/adr-scope.md:138`, `torch/cpu` is EXCLUDED as `Leaf with no architectural boundary`; and its immediate source-file line count violates the exclusion cap (252 exceeds limit 200). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `torch/cpu` COVERED and add `./src/torch/cpu/ADR.md`.
- In `src/adr-scope.md:139`, `torch/distributions` is EXCLUDED as `Leaf with no architectural boundary`; and its immediate source-file line count violates the exclusion cap (9901 exceeds limit 200). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `torch/distributions` COVERED and add `./src/torch/distributions/ADR.md`.
- In `src/adr-scope.md:140`, `torch/fft` is EXCLUDED as `Leaf with no architectural boundary`; and its immediate source-file line count violates the exclusion cap (1442 exceeds limit 200). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `torch/fft` COVERED and add `./src/torch/fft/ADR.md`.
- In `src/adr-scope.md:141`, `torch/func` is EXCLUDED as `Leaf with no architectural boundary`; and its immediate source-file line count violates the exclusion cap (296 exceeds limit 200). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `torch/func` COVERED and add `./src/torch/func/ADR.md`.
- In `src/adr-scope.md:142`, `torch/futures` is EXCLUDED as `Leaf with no architectural boundary`; and its immediate source-file line count violates the exclusion cap (335 exceeds limit 200). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `torch/futures` COVERED and add `./src/torch/futures/ADR.md`.
- In `src/adr-scope.md:146`, `torch/linalg` is EXCLUDED as `Leaf with no architectural boundary`; and its immediate source-file line count violates the exclusion cap (3174 exceeds limit 200). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `torch/linalg` COVERED and add `./src/torch/linalg/ADR.md`.
- In `src/adr-scope.md:147`, `torch/masked` is EXCLUDED as `Leaf with no architectural boundary`; and its immediate source-file line count violates the exclusion cap (3074 exceeds limit 200). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `torch/masked` COVERED and add `./src/torch/masked/ADR.md`.
- In `src/adr-scope.md:151`, `torch/multiprocessing` is EXCLUDED as `Leaf with no architectural boundary`; and its immediate source-file line count violates the exclusion cap (1238 exceeds limit 200). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `torch/multiprocessing` COVERED and add `./src/torch/multiprocessing/ADR.md`.
- In `src/adr-scope.md:152`, `torch/nativert` is EXCLUDED as `Leaf with no architectural boundary`; and its immediate source-file line count violates the exclusion cap (239 exceeds limit 200). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `torch/nativert` COVERED and add `./src/torch/nativert/ADR.md`.
- In `src/adr-scope.md:153`, `torch/nested` is EXCLUDED as `Leaf with no architectural boundary`; and its immediate source-file line count violates the exclusion cap (522 exceeds limit 200). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `torch/nested` COVERED and add `./src/torch/nested/ADR.md`.
- In `src/adr-scope.md:154`, `torch/numa` is EXCLUDED as `Leaf with no architectural boundary`; and its immediate source-file line count violates the exclusion cap (735 exceeds limit 200). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `torch/numa` COVERED and add `./src/torch/numa/ADR.md`.
- In `src/adr-scope.md:155`, `torch/package` is EXCLUDED as `Leaf with no architectural boundary`; and its immediate source-file line count violates the exclusion cap (3320 exceeds limit 200). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `torch/package` COVERED and add `./src/torch/package/ADR.md`.
- In `src/adr-scope.md:156`, `torch/quantization` is EXCLUDED as `Leaf with no architectural boundary`; and its immediate source-file line count violates the exclusion cap (589 exceeds limit 200). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `torch/quantization` COVERED and add `./src/torch/quantization/ADR.md`.
- In `src/adr-scope.md:158`, `torch/sparse` is EXCLUDED as `Leaf with no architectural boundary`; and its immediate source-file line count violates the exclusion cap (12622 exceeds limit 200). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `torch/sparse` COVERED and add `./src/torch/sparse/ADR.md`.
- In `src/adr-scope.md:159`, `torch/special` is EXCLUDED as `Leaf with no architectural boundary`; and its immediate source-file line count violates the exclusion cap (1459 exceeds limit 200). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `torch/special` COVERED and add `./src/torch/special/ADR.md`.
- In `src/adr-scope.md:161`, `torch/xpu` is EXCLUDED as `Build/config only`; and its immediate source-file line count violates the exclusion cap (3182 exceeds limit 2000). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `torch/xpu` COVERED and add `./src/torch/xpu/ADR.md`.
- In `src/adr-scope.md:163`, `torchgen/_autoheuristic` is EXCLUDED as `Leaf with no architectural boundary`; and its immediate source-file line count violates the exclusion cap (2084 exceeds limit 200). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `torchgen/_autoheuristic` COVERED and add `./src/torchgen/_autoheuristic/ADR.md`.
- In `src/adr-scope.md:166`, `torchgen/operator_versions` is EXCLUDED as `Leaf with no architectural boundary`; and its immediate source-file line count violates the exclusion cap (397 exceeds limit 200). Replace this invalid exclusion with a valid approved classification; if no approved exclusion reason fits, mark `torchgen/operator_versions` COVERED and add `./src/torchgen/operator_versions/ADR.md`.
- `aten/src/ATen/cpu/vec` is named as a book subsystem at book/04-aten-tensor-library.md:158, book/04-aten-tensor-library.md:274 but has no COVERED ancestor. Add coverage by marking `aten/src/ATen/cpu/vec` COVERED and writing `./src/aten/src/ATen/cpu/vec/ADR.md`, or by adding a new COVERED ancestor ADR that explicitly covers this subsystem.
- `functorch` is still EXCLUDED but is named as a book subsystem at book/_generated/architecture-map.md:41. Change `adr-scope.md` so `functorch` is COVERED and add `./src/functorch/ADR.md`.
- `torch/csrc` is named as a book subsystem at book/13-performance-architecture.md:28 but has no COVERED ancestor. Add coverage by marking `torch/csrc` COVERED and writing `./src/torch/csrc/ADR.md`, or by adding a new COVERED ancestor ADR that explicitly covers this subsystem.
